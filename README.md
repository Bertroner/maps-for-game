# Maps for Game

Public, versioned map assets and technical import manifests for tabletop campaigns.

Each campaign lives in its own directory. Source canon, quest guides, secrets, internal prompts, and GM-only working data must never be committed here.
